5.times { |time|
    puts time
}
# Здесь метод .times отдает значение переменной |time|. Но имейте в виду, что эта переменная известна только внутри блока
# Метод .time выполняет блок int раз, передавая в него значения от 0 до int - 1.
puts

9.times { |time|
    puts time
}